1. `const a=1,b=2;`のようには書けるのに`export const a=1,b=2;`のようには書けないの？
2. `startsWith`と`.test`をわざわざ二つ書く必要性がどこにあるのか分からない。`return s.trim().startsWith('class') || /^class\s/.test(s);`とあるが`toString`が継承で変更された場合、どのみち後者`test(s)`を追加しても無駄なのでは？　
3. `isRef(null)`=`true`は正しい。`null`は参照型ではない。そもそもJSには参照型など存在しない。型が存在しないから。よって`null`をプリミティブ型と参照型のどちらに所属させるかという公式な答えは「JSには型など存在しない」という破綻した答えしか存在しない。一応JSではnullはプリミティブの一つに分類される。しかし`typeof`では`"object"`を返す。しかし実際はObjectではないしプロパティも持てないプリミティブ値であるという矛盾した挙動をする。ちなみにプリミティブ型と参照型の両方あるJavaではnullはどちらに分類されるの？


1. `const a=1,b=2;`のようには書けるのに`export const a=1,b=2;`のようには書けないの？

私は以下のように書いたのだが。最後のセミコロンが読み取れなかったのか？　念の為にフェンスで囲んで再投稿する。なぜか以下でエラーが出るのだが。`,`区切りで書けるはずだよね？

```javascript
export const getId = (v) => Object.prototype.toString.call(v).slice(8, -1),
    isPri = (v)=>v !== Object(v),
    isRef = (v)=>v === Object(v),
    anyExe = (v) => 'function' === typeof v,
    isClsStyle = (v) => (v?.toString() ?? '').trim().startsWith('class'),
    isCls = (v) => anyExe(v) &&  isClsStyle(v),
    isExe = (v) => anyExe(v) && !isClsStyle(v),
;
```

2. `startsWith`と`.test`をわざわざ二つ書く必要性がどこにあるのか分からない

`return s.trim().startsWith('class') || /^class\s/.test(s);`とあるが、`startsWith('class')`と`/^class\s/.test(s);`の内容はほぼ同じだと思うのだが。スペース必須にすべき、という意味か？

3. `isRef(null)`=`true`なら間違っていた。私が期待するのは`null`はプリミティブ型だった。

ということは、`isRef = (v)=>null!==v && v===Object(v)`にすべきか？





1. export const のカンマ区切りについて

つまり最後の要素に関してはカンマをつけたらダメってこと？　以下のようなノリで最後の要素でもカンマを付けたかったのだけど。場所によってカンマを付けたり消したりせねばならないのは統一性がないから嫌なので。でも、それができないってこと？

```
{
  k:1,
}
```

2. startsWith と test の違い

スペース必須ということなら`/^class\s/.test(s);`だけにすべきでは？　なぜ`return s.trim().startsWith('class') || /^class\s/.test(s);`などという間違ったものを許容するコードを残したの？

3. isRef(null) を false にする修正

私が書いたコードと同じだよね？　なぜ書き直したの？　ヨーダ記法は嫌いなの？



1. export const の末尾カンマについて

最後の末尾にカンマをつけることは不可能ってことね？

> 統一感を優先したい場合は、カンマ区切りをやめて、一つずつ export const を書くのが一般的です。

いやいや、問題は最後の末尾にカンマを付けることだよね？　じゃあ以下のように最初にカンマをつければ良いのでは？

```
export const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v)=>v !== Object(v)
    , isRef = (v)=>v === Object(v)
    , anyExe = (v) => 'function' === typeof v
    , isClsStyle = (v) => (v?.toString() ?? '').trim().startsWith('class')
    , isCls = (v) => anyExe(v) &&  isClsStyle(v)
    , isExe = (v) => anyExe(v) && !isClsStyle(v)
;
```

2. startsWith と test の併用について

> test だけで十分です。

ですよね？

/^class\s/.test(v?.toString() ?? '')

> 前回の回答で「併用」を提示したのは、万が一の正規表現のコストを懸念しての折衷案でしたが、ロジックとして重複しており、スマートではありませんでした。isClsStyle = (v) => /^class\s/.test(v?.toString() ?? '') とする方が簡潔で、意図も明確です。

いや違うでしょ。`return s.trim().startsWith('class')`のほうは`classy`などもヒットしてしまうからダメってお前言ってたでしょ。だからスマートじゃなくて、間違いだから書いたらダメなの。`startsWith`は削除して`.test()`だけにすべきか、あるいは`startsWith('class ')`のようにスペースを入れるべき。正規表現のコストうんぬん言うならばそうすべきでは？





```
export const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v)=>v !== Object(v)
    , isRef = (v)=>v === Object(v)
    , anyExe = (v) => 'function' === typeof v
    , isClsStyle = (v) => (v?.toString() ?? '').trim().startsWith('class ')
    , isCls = (v) => anyExe(v) &&  isClsStyle(v)
    , isExe = (v) => anyExe(v) && !isClsStyle(v)
;
```


1. export const のカンマ配置について

よし、ならこれで行く。コピペする時、フォーマットをそのまま使える。最後の要素だけカンマからセミコロンに変えねばならないようなのはミスるので嫌だからね。

2. startsWith と test の判定について

> 結論：startsWith('class') は誤判定を招くため、削除して .test() のみにすべきです。

いやいや、`startsWith('class ')`にすべきじゃないの？　問題はスペース必須なのにスペースが含まれていなかったことだよね？　そして正規表現はパフォーマンス的に不利だと。この両方を考慮するならば`startsWith('class ')`にすべきという答えになるはずでしょ？　スペースがあれば良いのであって、スペースがあるなら`startsWith`でも`test`でもどっちでも同じことだよね？　お前が言うには`startsWith`のほうがパフォーマンス的に有利なんでしょ？

最終的には以下のようなコードになるはず。





ああ、null判定が漏れていたか。助かった。

```
export const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v)=>null === v || v !== Object(v)
    , isRef = (v)=>null !== v && v === Object(v)
    , anyExe = (v) => 'function' === typeof v
    , isClsStyle = (v) => (v?.toString() ?? '').trim().startsWith('class ')
    , isCls = (v) => anyExe(v) &&  isClsStyle(v)
    , isExe = (v) => anyExe(v) && !isClsStyle(v)
;
```

> class{} のように、スペースを入れないクラス定義（JSでは有効）も判定対象に含める必要がありますか？
> ミニファイ（圧縮）されたコード（クラス名が1文字になる等）に対してもこの判定を使用する予定はありますか？

当然ある。だってクラスかどうか判定したいのだから。スペースの有無だのミニファイ是非だのは関係ない。どんなコードであれそれがクラスなら真を返し、そうでないなら偽を返したい。それがクラスなのに偽を返されたら困るし、それがクラスでないのに真を返されても困る。

書体なんて関係ない。JSに型判定する機能がないか、あっても役立たずかバグだらけで使えないから、仕方なくこういうやり方をしているだけだ。

あのさ、まさかとは思うけど、他にもisClsStyleに何か抜け漏れある？　このコードは他のセッションでジェミニに書いてもらった奴なんだけど……。



1. isRef の論理演算ミス（重要）

ほんとだ。コピペミスだ。

こういうことが怖いので`isRef = (v)=>!isPri(v)`のようにしても良いかと思うのだが。パフォーマンスと保守性どちらを取るかのトレードオフになりそう。

2. isClsStyle の「抜け漏れ」

```
// classの直後に「空白」か「{」か「/（コメント開始）」が来るものを許可
, isClsStyle = (v) => /^class[\s{/]/.test((v?.toString() ?? '').trim())
```

なるほどね。でもスペースのあとに`/`や`{`があるものに関してはヒットしないのでは？

もう実行コスト度外視でいいから正確性を優先してくれ。正確でなければパフォーマンス重視する意味も価値もない。間違ったものを高速化してもゴミでしかないからだ。正確性こそが最優先すべきに決まっている。

ES5時代のクラスもどきは`function`オブジェクトでクラスを表現していた。ES6時代でも内部的には同じだが、実装時のコードが文字列で取得できるため`class`というキーワードで区別可能。そういう認識で合っているよね？

関数に見えるのは当然だ。JSにはクラスなど存在しない。関数オブジェクトをクラスと呼んでいるだけだ。それはプロトタイプベースという劣等言語を、クラスベースという高級言語に見せかける欺瞞工作だ。プロトタイプベースはスパゲッティコードになるし、型もないならもうプログラミング言語として致命的な欠陥品だ。しかしブラウザで動作する唯一のプログラミング言語なので避けることもできない。地獄である。

よってBabelでES5にトランスパイルされたES6の`class`が`function`になってしまうのは言語仕様的に避けられない。それはもう`isCls`で`false`を返す挙動になってしまうが仕方ない。

また、Proxyに関しては新しいライブラリのくせに、`a instanceof Proxy`のようにもできない。

組込型に関しても扱いに困る。`Date`や`RegExp`はNativeCodeであり、その実体はC言語と思われる。よって彼らをクラスとして扱うのは間違いだろう。恐らくJS的には関数オブジェクトだ。ES5クラスもどきと思われる。困ったことに`new`を使えるためインスタンス生成できるように見えるから、`class`構文で書かれたクラスと見分けが付かない。さて、彼らをクラスとして扱うべきか？　分からない。JS公式的にはどういう扱いなの？　多分クラスではないと思うのだけど。


1. isRef の保守性重視

ならそうするか。迷ったら保守性重視が安牌だ。

2. isClsStyle の正確性（スペースの後の考慮）

`(v?.toString() ?? '')`にして以下のようにした。

```
export const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !Pri(v)
    , anyExe = (v) => 'function' === typeof v
    // class の後に「1文字以上の空白」または「コメント」または「直接 { 」が続く
    , isClsStyle = (v) => /^class(?:\s|(\/\*[\s\S]*?\*\/)|{)/.test((v?.toString()?.trim() ?? ''))
    , isCls = (v) => anyExe(v) &&  isClsStyle(v)
    , isExe = (v) => anyExe(v) && !isClsStyle(v)
;
```

ところで、上記のコードは`/**/`という複数行コメントには対応しているが単行コメント`//`には対応していないのでは？　以下のような場合でもヒットする？（普通は書かないと思うが）

```
class // コメント
{ 
}
```

あとさ、コメントはJSDoc`/** */`の形式もあると思う。そっちは大丈夫そうかな？　基本的に同じ書式のはずだし。

他には抜け漏れは無さそうかな？

3. 組み込み型（Date, RegExp等）の正体

> function Date() { [native code] }

上記のような`[native code]`というキーワードを返すなら、それはもう立派な型の一種だろう。ただしクラスという型ではなく、ネイティブコードという型だ。どう実装すれば良いと思う？

```
isNativeCode = (v)=>...
```
----------------

1. isClsStyle のコメント対応（単一、複数、JSDoc）

ああそうか。` /* 1 */ // 2`のような形式もありえるか。

```
    // コメントと空白をすべてスキップして、その次が '{' か 'クラス名の始まり' かをチェック
    return /^class(?:\s|\/\/.*|\/\*[\s\S]*?\*\/)*[\s{]/.test(s.trim());
```

うーん、`isClsStyle`の`最後が空白か { で終わる`っていうのさ、最後が空白で終わることはありえないのでは？　だって`trim()`しているから。でもだからって最後が必ず`{`で終わるとは限らないのか？改行コードで終わる場合もある。でもそれは`trim()`で消えるはず。どう思う？

```
export const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !isPri(v)
    , anyExe = (v) => 'function' === typeof v
    // class直後の空白・コメント(単一/複数)を許容し、最後が空白か { で終わる
    , isClsStyle = (v) => /^class(?:\s|\/\/.*|\/\*[\s\S]*?\*\/)*[\s{]/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    , isCls = (v) => anyExe(v) &&  isClsStyle(v)
    , isExe = (v) => anyExe(v) && !isClsStyle(v)
    , isNtv = (v) => anyExe(v) &&  isNtvStyle(v)
;
```

`bound function`は`[native code]`になるのか。それは組込型ではなくユーザが自分で`bind()`したもののはず。ユーザ的には`[native code]`という感覚ではないはず。

`bound function`は`isExe`で`true`を返すはずだよね？　`isCls`で`false`、`isNativeCode`で`true`を返しちゃう。うーん。

JS的にはクラスも関数もネイティブコードも全部「実行可能」でありFunctionオブジェクトなのは分かる。でも、それを区別したい。どう区別すべきか。これが問題だ。

試しに分類の構造を考えてみた。しかしこれ、めちゃくちゃ大変そうだ。なので後で投稿する。今は`isClsStyle`を完成させよう。




1. isClsStyle のブラッシュアップ

一気にシンプルになったな。単語境界`\b`とは`[_a-zA-Z]`でない文字のことで合っている？　その場合、必ず` `,`/`,`{`,`\n`のいずれかが直後に入ってくる想定だから問題ないという認識で合っている？

```
// 「class」という単語として独立しており、かつその後に「classy」などが続かないことを保証
, isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
```

2. bound function と Native Code のジレンマ

bind()されたら絶対`[native code]`になっちゃうのは対処不能。だから組込型かbind()関数かを区別する方法は文字列判定`v.name.startsWith('bound ')`しかないと。

```
export const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !Pri(v)
    , anyExe = (v) => 'function' === typeof v
    // 「class」という単語として独立しており、かつその後に「classy」などが続かないことを保証
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    , isBndStyle = (v) => v.name.startsWith('bound ')
    , isCls = (v) => anyExe(v) &&  isClsStyle(v)
    , isExe = (v) => anyExe(v) && !isClsStyle(v)
    , isNtv = (v) => anyExe(v) &&  isNtvStyle(v)
    , isBnd = (v) => anyExe(v) &&  isNtvStyle(v) &&  isBndStyle(v)
    , isBlt = (v) => anyExe(v) &&  isNtvStyle(v) && !isBndStyle(v)
;
```

つまりネイティブコードは`[native code]`になるもの全般。その中には組込型もあるが`bind()`されたもの全般も含む。これを区別するには`v.name.startsWith('bound ')`で判定可能。その是非によってBoundか組込型(Builtin)かを判定できるってことだよね？




`Object.defineProperty`でメタプログラミングされちゃあ仕方ないね。どうせ元々JSは型がないという致命的な欠陥があるわけだし。それをエンドユーザがその欠陥言語を使ってフォローすることなど原理的に不可能だし。メタられたら諦めるしかない。

さて、めちゃくちゃ大変そうな分類について以下に投稿する。

```
Primitive
	Undefined
	Null
	Boolean
	Number
	BigInt
	String
	Symbol
Reference
	Owner
		Class		class構文で生成されたもののみ
		Instance	newで生成されたもののみ
		Object		{}で生成されたもののみ
		Array
		Set
		Map
		Iterator
		Generator
		Descriptor
			Accessor
				Getter
				Setter
				Getter
			Data
				Value
				Executable
		他に抜け漏れはないか？（尚プリミティブ型のオブジェクト版は一旦存在しないものとする）
	Executable
		NativeCode			組込型／bind()
			BoundFunction	bind()
			BuiltinFunction	非bind()
		Functionable
			Function
			AsyncFunction
			GeneratorFunction
			AsyncGeneratorFunction
		ArrowFunctionable
			ArrowFunction
			AsyncArrowFunction
		Methodable
			Method
			AsyncMethod
			GeneratorMethod
			AsyncGeneratorMethod
```

各分類間で排他的にしたい。つまりどれか一つだけが真になるようにしたい。（但し親は子らを包含し、いずれか一つでも真なら真を返すAPIも用意したい。`isExe()等`）

問題は`Owner`の実体は関数オブジェクトのはずだから`Executable`実行可能であることだ。`Executable`のほうは非Ownerな実行可能のものということになる。となると、`Executable`という名前が紛らわしい。実際、`Owner`も`new`により実行可能なのだから。

しかし`new`で実行するコンストラクタは特殊な関数という扱いなので、区別すべきと言える。少なくともクラスベース言語なら区別しているはずだ。

というわけで上記のように分類した。恐らく`Date`なら`isNativeCode`が真を返すし、`new Date()`なら`Instance`が真を返すはず。

組込型なら`isNativeCode`になるし、ES5クラスもどきなら`Function`が真を返すかな？　そしてそれは`function someFn() {}`と区別が付かない。恐らく現代では関数は`const someFn = (v)=>{};`のようにアロー関数で実装すべきだろう。これなら型で区別可能だ。しかし実際には好きに書けてしまう。

* Executableの名前をどうするか（Functionableよりはマシだと思うが大差ない。Owner.Classと区別するのに最適な名前は無いものか？）
* 他に抜け漏れはないか？

抜け漏れだが、多分これで大分類は可能だと思うが、どう思う？










> JSの型システムが抱える「すべてはオブジェクト（またはプリミティブ）」というカオスな構造を、クラスベース言語の視点できれいに整理しようとする素晴らしい試みです。

もし本当にすべてはオブジェクトなら良かったのにね。実際はプリミティブとオブジェクトに分かれているせいで、その主張すら嘘なんだけど。JSは嘘や矛盾だらけのクソ言語。


1. 「Executable」の改名案

`Callable`か。いいね。

* Functionable（NativeCodeも含むから微妙か）
* Executable（Class(Constructor)は除外したいから微妙か）
* Callable（Executableと同じだがそれより短い）

お前の以下の定義は、私のイメージしているものだが、JS的にはCallableなFunctionもnewできちゃうよね。（アロー関数はnewできないのかもしれないけど）　まあ、どうしようもないのだけど。

> Owner (Constructable): new 演算子でインスタンス化するもの。
> Callable (Only Call): new できず、単に関数として呼び出すもの（アロー関数、メソッドなど）。

2. 抜け漏れの指摘

それらはどうしようかなぁ。ErrorはErrorClassとErrorInstanceという区分で判定できるようにしたい。

ただ、それ以外は`instanceOf(ins, Class)`で良い気もする。ぶっちゃけ純粋に真偽値を返すだけなら`ins instanceof Class`句を使えば良いのだが。一応ね。

だって他にもWindow/Document/Node/Element/HTMLElement/SVGElementとか色々欲しくなりそうだし。




-------------


お前のConstructableの構造は成立不能では？　お前自身`ES5までの function A(){}。判定不能だがFunctionから分離したい意図`と書いてあるし。私も区別できるならしたいが。できないので諦めた。もうES5クラスもどきは`Function`でいいと割り切る。ぶっちゃけES5クラスもどきは使わないで欲しい。

また、お前は`[[Constructor]]`,`[[Call]]`というJS内部エンジンの名前にあやかって命名したようだが、もしJS内部エンジンと同じように分類するならば、ConstructorとCallはExecutableという親に包含されるのでは？　違うのかな？

どう分類すべきなのか。JS内部エンジンに合わせるべきか？　しかしそれがどう分類されているかを私は知らない。知らないから判断できない。そのあたり教えてくれ。

というわけで、今はまだ私の分類基準にしておく。但しinstanceあたりが怪しいのでちょっと変えた。

```
Primitive			何のメンバも持てない（new Number()は対象外）
	Undefined
	Null
	Boolean
	Number
	BigInt
	String
	Symbol
Reference
	Owner			何らかのメンバを持っている（Functionオブジェクトもそうなのだが、それはES5クラスもどきであり対象外とする）
		Class		class構文で生成されたもののみ（コンストラクタ）
		Instance	newで生成されたもののみ
			of(Cls)		引数で指定された任意クラスのインスタンスなら真を返す
			// 以降は特別枠でof()でなく名前がついたAPIを用意する（コンテナとしてよく使うから）
			Object		{}で生成されたもののみ
			Array
			Set
			Map
	Iterator
	Generator
	Descriptor
		Accessor
			Getter
			Setter
			Getter
		Data
			Value
			Executable
	Callable				コンストラクタを除いた実行可能なもの
		NativeCode			組込型／bind()
			BoundFunction	bind()
			BuiltinFunction	非bind()
		Functionable
			Function
			AsyncFunction
			GeneratorFunction
			AsyncGeneratorFunction
		ArrowFunctionable
			ArrowFunction
			AsyncArrowFunction
		Methodable
			Method
			AsyncMethod
			GeneratorMethod
			AsyncGeneratorMethod
	Error
		Class
		Instance
```

とりあえず仮実装してみた。

```javascript
const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !Pri(v)
    , anyCal = (v) => 'function' === typeof v
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    , isBndStyle = (v) => v.name.startsWith('bound ')
    , isCls = (v) => anyCal(v) &&  isClsStyle(v)
    , isCal = (v) => anyCal(v) && !isClsStyle(v)
    , isNtv = (v) => anyCal(v) &&  isNtvStyle(v)
    , isBnd = (v) => anyCal(v) &&  isNtvStyle(v) &&  isBndStyle(v)
    , isBlt = (v) => anyCal(v) &&  isNtvStyle(v) && !isBndStyle(v)
;
export const typer = Object.freeze({
	id: getId,
	p: {
		is: isPri,
		und:(v)=>undefined===v,
		nul:(v)=>null===v,
		bln:(v)=>'boolean'===typeof v,
		num:(v)=>'number'===typeof v,
		big:(v)=>'bigint'===typeof v,
		str:(v)=>'string'===typeof v,
		sym:(v)=>'symbol'===typeof v,
	}, 
	r: {
		is: isRef,
		own: {
			cls: isCls,
			ins: {
				of: (v, c)=>v instanceof c,
				obj: (v)=>Object===v?.constructor,
				ary: (v)=>Array.isArray(v),
				set: (v)=>v instanceof Set, // 'Set'===getId(v)
				map: (v)=>v instanceof Map, // 'Map'===getId(v)
			}
		},
		itr: (v)=>,
		gen: (v)=>,
		des: {
			a: {// accessor
				g: (v)=>,  // getter
				s: (v)=>,  // setter
				gs: (v)=>, // getter & setter
			}, 
			d: {// data
				v: (v)=>!isCal(v), //value
				c: (v)=> isCal(v), //callable
			}
		}
		cal: {
			is: isCal,
			ntv: {
				is: isNtv,
				bnd: isBnd,
				blt: isBlt,
			},
			fun: {
				f:(v)=>,
				a:(v)=>,
				g:(v)=>,
				ag:(v)=>,
			},
			arw: {
				f:(v)=>,
				a:(v)=>,
			},
			met: {
				f:(v)=>,
				a:(v)=>,
				g:(v)=>,
				ag:(v)=>,
			}
		},
		err: {
			cls: (v)=>,
			ins: (v)=>v instanceof Error, // getId(v).endsWith('Error')
		},
	},
});
```




```
// 補助ユーティリティ
const isFn = (v) => 'function' === typeof v;
const getS = (v) => v?.toString?.() ?? '';

export const typer = Object.freeze({
    // ...既存部分は省略...
    r: {
        is: isRef,
        own: {
            cls: isCls,
            ins: {
                of: (v, c) => v instanceof c,
                // Object.create(null)で生成された「純粋な辞書」も考慮するなら v?.constructor === Object || Object.getPrototypeOf(v) === null
                obj: (v) => Object === v?.constructor,
                ary: (v) => Array.isArray(v),
                set: (v) => v instanceof Set,
                map: (v) => v instanceof Map,
            }
        },
        // Iterator: nextメソッドを持つオブジェクト
        itr: (v) => isRef(v) && isFn(v.next),
        // Generator (実行中オブジェクト): getIdが [object Generator] になるもの
        gen: (v) => 'Generator' === getId(v),
        des: {
            // Descriptorの判定は、対象オブジェクト(obj)とキー(k)が必要になるため単体判定は困難ですが、
            // 構造チェック（valueかget/setを持つか）で代用します
            a: { // Accessor
                g: (v) => isFn(v?.get) && !isFn(v?.set),
                s: (v) => !isFn(v?.get) && isFn(v?.set),
                gs: (v) => isFn(v?.get) && isFn(v?.set),
            },
            d: { // Data
                v: (v) => v !== null && Object.prototype.hasOwnProperty.call(v, 'value') && !isFn(v.value),
                c: (v) => isFn(v?.value), // valueが関数ならExecutable
            }
        },
        cal: {
            is: isCal,
            ntv: { is: isNtv, bnd: isBnd, blt: isBlt },
            fun: { // functionキーワードで定義されたもの
                f: (v) => isCal(v) && /^function\s/.test(getS(v)),
                a: (v) => isCal(v) && /^async\s+function\b/.test(getS(v)),
                g: (v) => isCal(v) && /^function\s*\*/.test(getS(v)),
                ag: (v) => isCal(v) && /^async\s+function\s*\*/.test(getS(v)),
            },
            arw: { // () => ...
                f: (v) => isCal(v) && !/^function\b/.test(getS(v)) && getS(v).includes('=>'),
                a: (v) => isCal(v) && /^async\s*\(/.test(getS(v)) && getS(v).includes('=>'),
            },
            met: { // オブジェクト内のメソッド shorthand: m() { ... }
                // toStringが "m() { ... }" のように functionキーワード無しで始まるもの
                f: (v) => isCal(v) && !/^(?:async\s+)?function\b/.test(getS(v)) && !getS(v).includes('=>'),
                a: (v) => isCal(v) && /^async\s+/.test(getS(v)) && !getS(v).includes('function') && !getS(v).includes('=>'),
                // Generator Methodなどはさらに複雑なため、必要に応じて正規表現を調整
            }
        },
        err: {
            cls: (v) => isCls(v) && (v.prototype instanceof Error || v === Error),
            ins: (v) => v instanceof Error,
        },
    },
});
```



うーん、なぜ文字列で型判定しようとするのか。文字列を使わずとも判定可能なはずだが。そちらのほうが確実なはずだが。AsyncFunctionとかもっと簡単に判定できるはずだが。

とりあえずPrimitive/Object/Arrayの所を修正した。

```javascript
const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , getS = (v) => v?.toString?.() ?? ''
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !Pri(v)
    , isFn = (v) => 'function' === typeof v
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    , isBndStyle = (v) => v.name.startsWith('bound ')
    , isCls = (v) => isFn(v) &&  isClsStyle(v)
    , isCal = (v) => isFn(v) && !isClsStyle(v)
    , isNtv = (v) => isFn(v) &&  isNtvStyle(v)
    , isBnd = (v) => isFn(v) &&  isNtvStyle(v) &&  isBndStyle(v)
    , isBlt = (v) => isFn(v) &&  isNtvStyle(v) && !isBndStyle(v)
    , isObj = (v) => Object===v?.constructor
    , pTypNms = 'Undefind Null Boolean Number BigInt String Symbol'.split(' '),
    , pClsOfNms = {Undefind:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}
	, isAry = (v) => Array.isArray(v)
;
const pri = {
    isUnd: (v) => undefined===v,
    isNul: (v) => null===v,
    isBln: (v) => 'boolean'===typeof v,
    isNum: (v) => 'number'===typeof v,
    isBig: (v) => 'bigint'===typeof v,
    isStr: (v) => 'string'===typeof v,
    isSym: (v) => 'symbol'===typeof v,
};
const priOfFn = (v, c) => {
	if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
	cont n = pTypNms.find(n=>n===c.name);
	if(!n) {throw new TypeError(`cはプリミティブ型であるべきです。実際値:${n} 期待値:${pTypNms}`)}
	return pri[`is${n}`];
};
const insOfFn = (v, c) => {
	if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
	return ((v, c)=>v instanceof c);
};
export const typer = Object.freeze({
	id: getId,
	p: {
		is: isPri,
		of: priOf,
		und:(v)=>pri.isUnd,
		nul:(v)=>pri.isNul,
		bln:(v)=>pri.isBln,
		num:(v)=>pri.isNum,
		big:(v)=>pri.isBig,
		str:(v)=>pri.isStr,
		sym:(v)=>pri.isSym,
	}, 
	r: {
		is: isRef,
		own: {
			cls: isCls,
			ins: {
				of: (v, c)=>v instanceof c,
				obj: isObj,
				ary: isAry,
				set: (v)=>v instanceof Set, // 'Set'===getId(v),
				map: (v)=>v instanceof Map, // 'Map'===getId(v),
				soloObj: (v)=>isObj(v) || Object.getPrototypeOf(v) === null,
				blankObj: (v)=>isObj(v) && 0===Object.keys(v).length,
				blankAry: (v)=>isAry(v) && 0===v.length,
				aryOf: (v, c)=>{// Array<T>
					const fn = isPri(c) ? priOfFn(v,c) : (isCls(c) ? insOfFn(v,c) : (()=>{throw new TypeError(`cはクラスであるべきです。`)})());
					if (!isAry(v)) {throw new TypeError(`vはArrayであるべきです。`)}
					return v.every(e=>fn(e, c));
				},
			}
		},
		itr: (v)=>isRef(v) && isFn(v.next),
		gen: (v)=>'Generator' === getId(v),
		des: {
			is: (n, o)=>,
			a: {// accessor
				g: (n, o)=>,  // getter
				s: (n, o)=>,  // setter
				gs: (n, o)=>, // getter & setter
			}, 
			d: {// data
				v: (n, o)=>!isCal(v), //value
				c: (n, o)=> isCal(v), //callable
			}
		}
		cal: {
			is: isCal,
			ntv: {
				is: isNtv,
				bnd: isBnd,
				blt: isBlt,
			},
			fun: {
				f:(v)=>isCal(v) && /^function\s/.test(getS(v)),
				a:(v)=>isCal(v) && /^async\s+function\b/.test(getS(v)),
				g:(v)=>isCal(v) && /^function\s*\*/.test(getS(v)),
				ag:(v)=>isCal(v) && /^async\s+function\s*\*/.test(getS(v)),
			},
			arw: {
				f:(v)=>isCal(v) && !/^function\b/.test(getS(v)) && getS(v).includes('=>'),
				a:(v)=>isCal(v) && /^async\s*\(/.test(getS(v)) && getS(v).includes('=>'),
			},
			met: {
				f:(v)=>isCal(v) && !/^(?:async\s+)?function\b/.test(getS(v)) && !getS(v).includes('=>'),
				a:(v)=>isCal(v) && /^async\s+/.test(getS(v)) && !getS(v).includes('function') && !getS(v).includes('=>'),
				g:(v)=>,
				ag:(v)=>,
			}
		},
		err: {
			cls: (v)=>isCls(v) && (v.prototype instanceof Error || v === Error),
			ins: (v)=>v instanceof Error, // getId(v).endsWith('Error')
		},
	},
});
```



DRYに書く工夫もしてくれ。

AIがエラーになる。

```javascript
const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !Pri(v)
    , isFn = (v) => 'function' === typeof v
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    , isBndStyle = (v) => v.name.startsWith('bound ') && v.hasOwnProperty('prototype')
    , isCls = (v) => isFn(v) &&  isClsStyle(v)
    , isCal = (v) => isFn(v) && !isClsStyle(v)
    , isNtv = (v) => isCal(v) && isNtvStyle(v)
    , isBnd = (v) => isCal(v) && isNtvStyle(v) &&  isBndStyle(v)
    , isBlt = (v) => isCal(v) && isNtvStyle(v) && !isBndStyle(v)
    , isFun = (v, n='') =>isCal(v) && `${n}Function`===getId(v),
    , isArrOrMethod = (v) => isCal(v) && !v.prototype && !isBnd(v);
    , isArrStyle = (v) => {
        const s = v.toString().trim();
        return /^(async\s+)?(\([^)]*\)|[a-zA-Z_$][\w$]*)\s*=>/.test(s);
    }
    , anyArrFn = (v) => isArrOrMethod(v) && isArrStyle(v)
    , isArrFn(v, n='') => anyArrFn(v) && `${n}Function` === getId(v)
    , anyMethod = (v) => isArrOrMethod(v) && !isArrStyle(v);
    , isMethod    = (v) => 'Function' === getId(v) && anyMethod(v);
    , isAMethod   = (v) => 'AsyncFunction' === getId(v) && anyMethod(v);
    , isGMethod   = (v) => 'GeneratorFunction' === getId(v) && anyMethod(v);
    , isAGMethod  = (v) => 'AsyncGeneratorFunction' === getId(v) && anyMethod(v);
    , isObj = (v) => Object===v?.constructor
    , pTypNms = 'Undefind Null Boolean Number BigInt String Symbol'.split(' '),
    , pClsOfNms = {Undefind:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}
	, isAry = (v) => Array.isArray(v)
    , getDes = (o, n)=>Object.getOwnPropertyDescriptor(o, n)
    , isDesV = (o, n)=>{const d=getDes(o, n); return d && !isFn(d.value)} 
    , isDesC = (o, n)=>{const d=getDes(o, n); return d && !isFn(d.value)}
    , hasAcc = (o, n, g, s) => {
        const d = getDes(o, n);
        return !!(g && d?.get || !g && !d?.get) && (s && d?.set || !s && !d?.set);
    },
;
const pri = {
    isUnd: (v) => undefined===v,
    isNul: (v) => null===v,
    isBln: (v) => 'boolean'===typeof v,
    isNum: (v) => 'number'===typeof v,
    isBig: (v) => 'bigint'===typeof v,
    isStr: (v) => 'string'===typeof v,
    isSym: (v) => 'symbol'===typeof v,
};
const priOfFn = (v, c) => {
	if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
	cont n = pTypNms.find(n=>n===c.name);
	if(!n) {throw new TypeError(`cはプリミティブ型であるべきです。実際値:${n} 期待値:${pTypNms}`)}
	return pri[`is${n}`];
};
const insOfFn = (v, c) => {
	if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
	return ((v, c)=>v instanceof c);
};
export const typer = Object.freeze({
	id: getId,
	p: {
		is: isPri,
		of: priOf,
		und:(v)=>pri.isUnd,
		nul:(v)=>pri.isNul,
		bln:(v)=>pri.isBln,
		num:(v)=>pri.isNum,
		big:(v)=>pri.isBig,
		str:(v)=>pri.isStr,
		sym:(v)=>pri.isSym,
	}, 
	r: {
		is: isRef,
		own: {
			cls: isCls,
			ins: {
				of: (v, c)=>v instanceof c,
				obj: isObj,
				ary: isAry,
				set: (v)=>v instanceof Set, // 'Set'===getId(v),
				map: (v)=>v instanceof Map, // 'Map'===getId(v),
				soloObj: (v)=>isObj(v) || Object.getPrototypeOf(v) === null,
				blankObj: (v)=>isObj(v) && 0===Object.keys(v).length,
				blankAry: (v)=>isAry(v) && 0===v.length,
				aryOf: (v, c)=>{// Array<T>
					const fn = isPri(c) ? priOfFn(v,c) : (isCls(c) ? insOfFn(v,c) : (()=>{throw new TypeError(`cはクラスであるべきです。`)})());
					if (!isAry(v)) {throw new TypeError(`vはArrayであるべきです。`)}
					return v.every(e=>fn(e, c));
				},
			}
		},
		itr: (v)=>isRef(v) && isFn(v.next),
		gen: (v)=>'Generator' === getId(v),
		des: {
			is: (n, o)=>Object.getOwnPropertyDescriptor(o, n),
			a: {// accessor
				g: (n, o)=>hasAcc(o, n, true, false),  // getter
				s: (n, o)=>hasAcc(o, n, false, true),  // setter
				gs: (n, o)=>hasAcc(o, n, true, true), // getter & setter
			}, 
			d: {// data
				v: (n, o)=>isDesV(o, n), //value
				c: (n, o)=>isDesC(o, n), //callable
			}
		}
		cal: {
			is: isCal,
			ntv: {
				is: isNtv,
				bnd: isBnd,
				blt: isBlt,
			},
			fun: {
				is:(v)=>isCal(v) && (!!v.prototype || isBnd(v)),
				f:(v)=>isFun(v),
				a:(v)=>isFun(v, 'Async'),
				g:(v)=>isFun(v, 'Generator'),
				ag:(v)=>isFun(v, 'AsyncGenerator'),
			},
			arw: {
				is: anyArrFn,
				f: (v) => isArrFn(v),
				a: (v) => isArrFn(v, 'Async'),
			},
			met: {
				is: anyMethod,
				f: isMethod,
				a: isAMethod,
				g: isGMethod,
				ag: isAGMethod,
			},
		},
		err: {
			cls: (v)=>isCls(v) && (v.prototype instanceof Error || v === Error),
			ins: (v)=>v instanceof Error, // getId(v).endsWith('Error')
		},
	},
});
```

> isBlt（組み込み関数） の判定で、Date や Math.max などの静的メソッドも網羅できていますが、これらを isBlt ではなく isNtv 側でまとめて扱う形で進めますか？

何を言っているか分からない。`isBlt(Date)`や`isBlt(Math.max)`で何を返すの？　それらが真を返し、かつ`isBnd(Date)`や`isBnd(Math.max)`が偽を返し、`is(Date)`や`is(Math.max)`は真を返すなら問題ない。お前は何が聞きたいの？　isNtvはisBndかisBltのいずれか一つが真なら真を返すことを意図している。isNtvとisBnd/isBltは親子関係だからだ。

いや、それよりも、そういえばBoundがNativeCodeの中に入っちゃっているよな。`bind()`したコーダー的にはNativeCodeでなくUserCodeのはずなのに。この構造はこのままで良いのかどうかだな。

しかし、もし変えるとなると以下のようになる。そして`ntv.is()`が偽を返したら、それは一体何boundなんだという話になる。このときboundがntvの中に入っていないのが違和感になりそうだ。

```
cal: {
	is: 
	ntv:
		is:
		blt:
	bnd:
	fun:
	arw:
	met:
}
```

もうboundも特殊な関数だし、ntvに含めちゃっていいか。

> Error の cls 判定において、カスタムエラークラス（class MyError extends Error {}）も true になるロジックにしていますが、組み込みのエラーのみに限定する必要はありますか？

あー、確かに区別したほうが良いか？　いや、これ以上は複雑化しすぎる。どう構造化していいかも分からないし。辞めておこう。

> Bun環境での toString の結果が、ブラウザと一部異なる（空白の入り方など）可能性があるため、正規表現の部分を \s* などでよりルーズにする調整は必要でしょうか？

え、そうなの？　それは困る。だがその前に、そもそもtoStringを使わずに済むような実装に変えることが最優先だ。文字列に頼らずとも判定できるような形で実装すべきだ。話はそれからだ。











全然DRYに書いてくれないな。私の書いたコードをわざわざ破壊し分解してまで重複コードを書くのはなぜ？　嫌がらせ？

お前のコードの以下内容は、勝手に条件を変えていないか？　特に通常関数にboundFnを含めているのがヤバイ。これはntvとfunで区別していたはず。なのに勝手に同一視されたら困る。これは明確なバグだ。私に無断でこっそりバグを仕込むのは辞めろ。

あと`_isType`って何。めちゃくちゃ分かりづらい名前だな。関数系の判定式なのに型全般に見える。名付けのセンス無さすぎる。

```
    // プレフィックス(n)を指定して「AsyncFunction」等の型名を判定
    , _isType = (v, n) => `${n}Function` === getId(v)
    // 通常の関数（prototypeを持つ、またはbindされている）
    , isFunBase = (v) => isCal(v) && (!!v.prototype || isBnd(v))
    // アローまたはメソッド（prototypeを持たず、bindでもない）
    , isArrOrMet = (v) => isCal(v) && !v.prototype && !isBnd(v)
```

以下で文句ないだろう。

```js
const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !Pri(v)
    , isFn = (v) => 'function' === typeof v
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    , isBndStyle = (v) => v.name.startsWith('bound ') && v.hasOwnProperty('prototype')
    , isCls = (v) => isFn(v) &&  isClsStyle(v)
    , isCal = (v) => isFn(v) && !isClsStyle(v)
    , isNtv = (v) => isCal(v) && isNtvStyle(v)
    , isBnd = (v) => isCal(v) && isNtvStyle(v) &&  isBndStyle(v)
    , isBlt = (v) => isCal(v) && isNtvStyle(v) && !isBndStyle(v)
    , isFun = (v, n='') =>isCal(v) && `${n}Function`===getId(v)
    , isArrOrMethod = (v) => isCal(v) && !v.prototype && !isBnd(v)
    , isArrStyle = (v) => {
        const s = v.toString().trim();
        return /^(async\s+)?(\([^)]*\)|[a-zA-Z_$][\w$]*)\s*=>/.test(s);
    }
    , anyArrFn = (v) => isArrOrMethod(v) && isArrStyle(v)
    , isArrFn = (v, n='') => anyArrFn(v) && `${n}Function` === getId(v)
    , anyMethod = (v) => isArrOrMethod(v) && !isArrStyle(v)
    , isMethod    = (v) => 'Function' === getId(v) && anyMethod(v)
    , isAMethod   = (v) => 'AsyncFunction' === getId(v) && anyMethod(v)
    , isGMethod   = (v) => 'GeneratorFunction' === getId(v) && anyMethod(v)
    , isAGMethod  = (v) => 'AsyncGeneratorFunction' === getId(v) && anyMethod(v)
    , isObj = (v) => Object===v?.constructor
    , pTypNms = 'Undefind Null Boolean Number BigInt String Symbol'.split(' ')
    , pClsOfNms = {Undefind:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}
    , isAry = (v) => Array.isArray(v)
    , getDes = (o, n)=>Object.getOwnPropertyDescriptor(o, n)
    , isDesV = (o, n)=>{const d=getDes(o, n); return d && !isFn(d.value)} 
    , isDesC = (o, n)=>{const d=getDes(o, n); return d && !isFn(d.value)}
    , hasAcc = (o, n, g, s) => {
        const d = getDes(o, n);
        return !!(g && d?.get || !g && !d?.get) && (s && d?.set || !s && !d?.set);
    }
;
const pri = {
    isUnd: (v) => undefined===v,
    isNul: (v) => null===v,
    isBln: (v) => 'boolean'===typeof v,
    isNum: (v) => 'number'===typeof v,
    isBig: (v) => 'bigint'===typeof v,
    isStr: (v) => 'string'===typeof v,
    isSym: (v) => 'symbol'===typeof v,
};
const priOfFn = (v, c) => {
	if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
	const n = pTypNms.find(n=>n===c.name);
	if(!n) {throw new TypeError(`cはプリミティブ型であるべきです。実際値:${n} 期待値:${pTypNms}`)}
	return pri[`is${n}`];
};
const insOfFn = (v, c) => {
	if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
	return ((v, c)=>v instanceof c);
};
export const typer = Object.freeze({
	id: getId,
	p: {
		is: isPri,
		of: (v,c)=>(priOfFn(v,c))(v,c),
		und:(v)=>pri.isUnd,
		nul:(v)=>pri.isNul,
		bln:(v)=>pri.isBln,
		num:(v)=>pri.isNum,
		big:(v)=>pri.isBig,
		str:(v)=>pri.isStr,
		sym:(v)=>pri.isSym,
	}, 
	r: {
		is: isRef,
		own: {
			cls: isCls,
			ins: {
				of: (v, c)=>v instanceof c,
				obj: isObj,
				ary: isAry,
				set: (v)=>v instanceof Set, // 'Set'===getId(v),
				map: (v)=>v instanceof Map, // 'Map'===getId(v),
				soloObj: (v)=>isObj(v) || Object.getPrototypeOf(v) === null,
				blankObj: (v)=>isObj(v) && 0===Object.keys(v).length,
				blankAry: (v)=>isAry(v) && 0===v.length,
				aryOf: (v, c)=>{// Array<T>
					const fn = isPri(c) ? priOfFn(v,c) : (isCls(c) ? insOfFn(v,c) : (()=>{throw new TypeError(`cはクラスであるべきです。`)})());
					if (!isAry(v)) {throw new TypeError(`vはArrayであるべきです。`)}
					return v.every(e=>fn(e, c));
				},
			}
		},
		itr: (v)=>isRef(v) && isFn(v.next),
		gen: (v)=>'Generator' === getId(v),
		des: {
			is: (n, o)=>Object.getOwnPropertyDescriptor(o, n),
			a: {// accessor
				g: (n, o)=>hasAcc(o, n, true, false),  // getter
				s: (n, o)=>hasAcc(o, n, false, true),  // setter
				gs: (n, o)=>hasAcc(o, n, true, true), // getter & setter
			}, 
			d: {// data
				v: (n, o)=>isDesV(o, n), //value
				c: (n, o)=>isDesC(o, n), //callable
			},
		},
		cal: {
			is: isCal,
			ntv: {
				is: isNtv,
				bnd: isBnd,
				blt: isBlt,
			},
			fun: {
				is:(v)=>isCal(v) && (!!v.prototype || isBnd(v)),
				f:(v)=>isFun(v),
				a:(v)=>isFun(v, 'Async'),
				g:(v)=>isFun(v, 'Generator'),
				ag:(v)=>isFun(v, 'AsyncGenerator'),
			},
			arw: {
				is: anyArrFn,
				f: (v) => isArrFn(v),
				a: (v) => isArrFn(v, 'Async'),
			},
			met: {
				is: anyMethod,
				f: isMethod,
				a: isAMethod,
				g: isGMethod,
				ag: isAGMethod,
			},
		},
		err: {
			cls: (v)=>isCls(v) && (v.prototype instanceof Error || v === Error),
			ins: (v)=>v instanceof Error, // getId(v).endsWith('Error')
		},
	},
});

```


いやDRYとバグ作り込みは全く別件だから。バグ作り込みはヤバすぎるから。マジで辞めろ。

あと`priOfFn`に関しては以下のように修正すべきだ。せっかく`pClsOfNms`を用意していたのに使っていなかった。

```
	return pri[`is${pClsOfNms[n]}`];
```

他に私のコードから変えた所はないか？　全部を最初から何度も読み直すの大変だから読んでないのだが。

> このコードにより、あなたが目指していた「JSの歪な仕様を逆手に取った、一切の妥協のない排他的型判定システム」が完成しました。

いや文字列判定している所とかNativeやBoundやBuiltinとか妥協まみれだよ。逆手に取るの意味わからないし。相変わらずJSに翻弄されている。こんなものを書かなきゃいけない時点で。きっと想定外のあれこれが出てきて、逆にデバッグで苦労するみたいな事態に発展するに違いない。もう予見できるわ。

コードを読み直した。

* `isDesC`のフラグにバグがあった
* `isMethod`をDRYに書き直した

それと`cal`の`ntv`/`fun`/`arw`/`met`が排他的かどうか問題だ。このあたり曖昧になっていたはずだ。

```
    , isFun = (v, n='') => isCal(v) && `${n}Function`===getId(v)
    , isArrOrMethod = (v) => isCal(v) && !v.prototype && !isBnd(v)
```

以下のように修正した。これで4者が排他的になるはず。

```
    , isFun = (v, n='') => isCal(v) && `${n}Function`===getId(v) && !isNtv(v)
    , isArrOrMethod = (v) => isCal(v) && !v.prototype && !isNtv(v)
```

いやバグだらけじゃねーか。何が完璧だよ。これまだ絶対なんかバグあるわ。




私のコードから変えた所ある？　ないならコード出さなくていいよ。だってお前がこっそりバグを作り込んでいる可能性があるから使いたくないもん。お前は改善点があればそれを指摘してくれ。

> getId(v) の結果: [object AsyncFunction] などの文字列が、実行環境（Bunの特定バージョンなど）で揺れないことを祈るか、さらに防衛策を張るか。

え、なにそれ困る。正確性を最重視するって言ったよね？　問題あるなら言ってよ。また書き直しじゃん。どんなコードだっけ？　確かわざわざ値を作ってた奴だよね？

> Methodの判定: anyMethod の !isArrStyle は「アロー関数の形をしていなければメソッドである」という消去法ですが、これが「普通の匿名関数」を誤爆しないか。

ああ、そういえばアロー関数の他に匿名関数もあったか。穴だらけじゃん。

`cal`の中には`ntv`/`fun`/`arw`/`met`の他にも`ano`(匿名アノニマス)も必要か。

匿名関数って`!v.prototype`が真になっちゃうの？　それが偽なら現状のコードでも問題ないと思うのだけど。

まずは匿名関数を区別する必要があるか、分類するならどこに含めさせるべきかの仕様を決定させるべきかな。私としては`fun`の中に含めて良いと思っているのだけど。現状、匿名クラスだって`isCls`で真を返すでしょ？　それと同じノリ。名前の有無だけの違いであり性質に違いがないならば、同じ扱いでいいと思う。

どのみち単体テストのときのテストデータのひとつにはしたい。




1. 匿名関数（Anonymous Function）の挙動について

funに分類されるなら問題なし。では、何故お前は誤爆しないか聞いてきたのか。そうした事態が起こりうるからではないのか？　私の環境でテストが合格しても、他の環境では失格になる可能性もあるのかな？

2. getId(v) の「文字列の揺れ」への防衛策

そうこれ。

```
const AsyncFunction = (async () => {}).constructor;
const GeneratorFunction = (function* () => {}).constructor;
const AsyncGeneratorFunction = (async function* () => {}).constructor;

// 判定式
, isFun = (v, type) => isCal(v) && !isNtv(v) && v.constructor === type
```

ついでに`fun.is`もバグってたので以下のように修正した。バグ多すぎ。

```
, anyFun = (v) => isCal(v) && !!v.prototype && !isNtv(v)
```
```
            fun: {
                is: anyFun,
                f:(v) => isFun(v, Function),
                a:(v) => isFun(v, AsyncFunction),
                g:(v) => isFun(v, GeneratorFunction),
                ag:(v) => isFun(v, AsyncGeneratorFunction),
            },
```

以下の箇所も`getDes`を使うよう修正。

```
        des: {
            is: (n, o) => !!getDes(o, n),
```

3. メソッド判定の「穴」

> 「将来的に prototype を持たない新しい実行可能オブジェクト」がJSに追加された場合、すべてメソッドに誤爆する

対案があるとすれば、ディスクリプタ同様ownerを渡すように修正することか？　でもそうすると`cal.is`にも第二引数としてownerを渡さねばならないことになる。そしてmethod以外はownerなんて存在しない。強いて言えばグローバルオブジェクトか？　ともかく整合性が取れなくなりそうな気がする。

それと`own`という構造が必要か怪しいな。削除すべきか否か。

## まとめ：今すぐ修正すべき改善ポイント

> priOfFn のタイポ修正: Undefined の i が抜けていたり、n.slice(0, 3) で Und / Nul を作る部分は、前述の通り pClsOfNms[n] を使う形に直すべきです。

何言ってるか分からない。Undefined の i って何？　以下のコードで問題ないよね？

```
    , pTypNms = 'Undefind Null Boolean Number BigInt String Symbol'.split(' ')
    , pClsOfNms = {Undefind:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}

const priOfFn = (v, c) => {
    if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
    const n = pTypNms.find(n=>n===c.name);
    if(!n) {throw new TypeError(`cはプリミティブ型であるべきです。実際値:${n} 期待値:${pTypNms}`)}
    return pri[`is${pClsOfNms[n]}`];
};
```

本当は全コードを投下したいけど、AIがエラー吐くからなぁ。

> isBndStyle のプロパティチェック: v.hasOwnProperty('prototype') よりも !('prototype' in v) の方が、プロトタイプチェーンを遡らないため意図に近いはずです。

何を言っているか分からない。

このコードも別のAIが書いたコードなんだよね。まずは話を整理しよう。

BoundFnなら`prototype`を持っているはず。その確認をするコードの話だよね？　`hasOwnProperty`はプロトタイプチェーンを遡らないよね？　だって`Own`だし。なら問題ないのでは？

テストに行く前にコードリーディングの時点でバグ発見しまくりだからなぁ。まだ早いかな。



ーーーーーーーーーーーー


## 1. 匿名関数（Anonymous）の「誤爆」懸念について

JSの仕様変更については、対処のしようがないな。トランスパイラについてはコードが勝手に改竄されるなら、その時点で保証外だ。

これの問題の本質って、匿名関数じゃなくてメソッド判定のほうにあるのでは？　ownerを渡さずに、それがメソッドであることを判定すること自体に無理があるって話なのでは？

匿名関数は普通に`fun`に属するんだよね？　なら問題はメソッド判定のほうでしょ。メソッドじゃないのにメソッドだと判定しちゃう可能性があるよって話だよね？

## 2. getId(v) の「文字列の揺れ」対策（重要）

うん、もう変えた。前回書いた通りにね。

## 3. priOfFn のタイポ指摘について

> 私が言った「i が抜けている」とは、Undefind という綴りについてです。
> 正しくは Undefined です（f の後に i が必要）。

私が書いたコードでiはちゃんとあるよね？　前回投稿を読み直してコピペしてきたのが以下。抜けてないと思うんだけど？　もっと前は抜けてたのか？

```
    , pTypNms = 'Undefind Null Boolean Number BigInt String Symbol'.split(' ')
    , pClsOfNms = {Undefind:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}

const priOfFn = (v, c) => {
    if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
    const n = pTypNms.find(n=>n===c.name);
    if(!n) {throw new TypeError(`cはプリミティブ型であるべきです。実際値:${n} 期待値:${pTypNms}`)}
    return pri[`is${pClsOfNms[n]}`];
};
```

## 4. isBndStyle と prototype の関係

> 通常の関数: prototype プロパティを持っています。
> bindされた関数: 実行すると prototype が消えます（undefined になります）。

え、そうなの？　じゃあフラグ反転しなきゃダメじゃん。`!v.hasOwnProperty('prototype')`にすべきだよね？　いや、違うか。それで前回お前はプロトタイプチェーンまで遡って存在しないことを確認すべきだと言ったのね？　つまり`!('prototype' in v)`にすべきだと。

ところでプロトタイプチェーンまで遡るべきなの？　`bind()`したらちゃんとプロトタイプチェーンまで遡って`prototype`を削除してくれるものなの？

## 次にすべきこと

> pTypNms の綴り修正: Undefined に統一。

私とお前が見ているコード、違うのでは？

> isFun の引数変更: 文字列の n ではなく、AsyncFunction 等のコンストラクタ実体を渡すように変更。

もうやった。

> isDesV と isDesC の重複修正: 前回、両方 !isFn になっていた部分の修正。

そうね。これはバグってたから修正したはず。

とりあえず共通関数だけ投稿しておく。

```
const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !Pri(v)
    , isFn = (v) => 'function' === typeof v
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    //, isBndStyle = (v) => v.name.startsWith('bound ') && v.hasOwnProperty('prototype')
    , isBndStyle = (v) => v.name.startsWith('bound ') && !('prototype' in v)
    , isCls = (v) => isFn(v) &&  isClsStyle(v)
    , isCal = (v) => isFn(v) && !isClsStyle(v)
    , isNtv = (v) => isCal(v) && isNtvStyle(v)
    , isBnd = (v) => isCal(v) && isNtvStyle(v) &&  isBndStyle(v)
    , isBlt = (v) => isCal(v) && isNtvStyle(v) && !isBndStyle(v)
    , AsyncFunction = (async () => {}).constructor
    , GeneratorFunction = (function* () => {}).constructor
    , AsyncGeneratorFunction = (async function* () => {}).constructor
    , isFun = (v, type) => isCal(v) && !isNtv(v) && v.constructor === type
    , anyFun = (v) => isCal(v) && !!v.prototype && !isNtv(v)
    , isArrOrMethod = (v) => isCal(v) && !v.prototype && !isNtv(v)
    , isArrStyle = (v) => {
        const s = v.toString().trim();
        return /^(async\s+)?(\([^)]*\)|[a-zA-Z_$][\w$]*)\s*=>/.test(s);
    }
    , anyArrFn = (v) => isArrOrMethod(v) && isArrStyle(v)
    , isArrFn = (v, n='') => anyArrFn(v) && `${n}Function` === getId(v)
    , anyMethod = (v) => isArrOrMethod(v) && !isArrStyle(v)
    , isMethod = (v, n='') => anyMethod(v) && `${n}Function` === getId(v)
    , isObj = (v) => Object===v?.constructor
    , pTypNms = 'Undefind Null Boolean Number BigInt String Symbol'.split(' ')
    , pClsOfNms = {Undefind:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}
    , isAry = (v) => Array.isArray(v)
    , getDes = (o, n) => Object.getOwnPropertyDescriptor(o, n)
    , isDesV = (o, n) => {const d=getDes(o, n); return d && !isFn(d.value)} 
    , isDesC = (o, n) => {const d=getDes(o, n); return d &&  isFn(d.value)}
    , hasAcc = (o, n, g, s) => {
        const d = getDes(o, n);
        return !!(g && d?.get || !g && !d?.get) && (s && d?.set || !s && !d?.set);
    }
;
const pri = {
    isUnd: (v) => undefined===v,
    isNul: (v) => null===v,
    isBln: (v) => 'boolean'===typeof v,
    isNum: (v) => 'number'===typeof v,
    isBig: (v) => 'bigint'===typeof v,
    isStr: (v) => 'string'===typeof v,
    isSym: (v) => 'symbol'===typeof v,
};
const priOfFn = (v, c) => {
    if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
    const n = pTypNms.find(n=>n===c.name);
    if(!n) {throw new TypeError(`cはプリミティブ型であるべきです。実際値:${n} 期待値:${pTypNms}`)}
    return pri[`is${pClsOfNms[n]}`];
};
const insOfFn = (v, c) => {
    if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
    return ((v, c)=>v instanceof c);
};
```


## 1. 綴りの間違い（重要）

いやいや、`f`の直後に`i`あるよ？　どこが間違って……

```
    あなたのコード: Undefind （fの直後にiがない）
    正しい綴り: Undefined
```

あー！　`i`がないというのが嘘なんだ。`e`がないんだわ。

正しくは「`n`の直後に`e`がない」んだよ。

お前は私がタイポしているのは気づいているのに、どこがどう間違っているのかが分かっておらず、間違えた指摘を提示していたんだ。私はそれを確認したのに間違っていないから、何いってんだこいつってなってたんだわ。

紛らわしすぎるわ。二重三重の間違いが重なっているじゃねーか。私とお前が間違いの二重奏してたわ。バカすぎる。バカとバカが会話したらこうなるんだな。頭悪すぎるよこの会話。

ともかく以下のように修正した。これで問題ないはず。

```
    , pTypNms = 'Undefined Null Boolean Number BigInt String Symbol'.split(' ')
    , pClsOfNms = {Undefined:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}
```

## 2. isBndStyle と prototype

なら`!('prototype' in v)`で確定だな。

## 3. メソッド判定の「匿名関数」誤爆の可能性

お前が誤爆の可能性を指摘したってことは「完璧に排他的」というのは嘘だよね？

精度を上げるにはownerオブジェクトを渡すしかないだろうなぁ。

## 4. 構文上の細かいミス

> isRef = (v) => !Pri(v): isPri ではなく Pri になっています。

ナイス指摘。修正した。

> AsyncGeneratorFunction の定義: async function* () => {} は構文エラーです。正しくは async function* () {} （矢印は不要）です。

私は何も考えずお前が提示した以下コードをコピペしたのだが。もしアロー関数が不要ならば他のも全部消すべきなの？　正しいコードを出せ。

```
    , AsyncFunction = (async () => {}).constructor
    , GeneratorFunction = (function* () => {}).constructor
    , AsyncGeneratorFunction = (async function* () => {}).constructor
```

今回のコードは以下だった。これで合ってる？　`yield`とかしなくても大丈夫？

```
    , AsyncFunction = (async () => {}).constructor
    , GeneratorFunction = (function* () {}).constructor
    , AsyncGeneratorFunction = (async function* () {}).constructor
```

> また、AsyncGeneratorFunction の定義部分で構文エラーが出るはずですが、お手元の環境（Bunなど）でこの定義行はエラーなしで通っていますか？

エラーだったわ。





## 1. 綴りの修正

OK。

## 2. Async/Generator 系の「種（タネ）」の正解

> ※ AsyncFunction だけはアロー関数が使えるのでそのままでも動きますが、統一感を持たせるなら全部 function キーワード形式にしてもOKです。

本当？　アロー関数で定義したらアロー関数として判定されてしまって通常関数としては判定できなくなる、とか無い？

## 3. メソッド判定の「完璧に排他的」の真実

> 将来の仕様: JSに「第3の、prototypeを持たない呼び出し可能オブジェクト」が追加されたら、それは anyMethod に吸い込まれます。
> メタプログラミング: Object.defineProperty で通常の関数から無理やり prototype を消去されたら、それはメソッドとして判定されます。

それをされちゃあ仕方ない。現状はこれで良しとするか。

でも、完全性を持たせるならば改善の余地はありそうだ。

* `(target)`: 現状の方法
* `(name, owner)`: 新しい方法

`is()`でなく`has()`にすれば良いだけな気もする。でも統一性を持たせたAPIにできなくなりそうだな。Asyncなどの属性違いがあるから。

```
        cal: {
            met: {
                is: anyMethod,
                f: (v) => isMethod(v),
                a: (v) => isMethod(v, 'Async'),
                g: (v) => isMethod(v, 'Generator'),
                ag: (v) => isMethod(v, 'AsyncGenerator'),
                has: (name, owner, isAsync, isGenerator)=>{},
            },
```

ならば`r`の直下に新設するか？　`cal`直下にもあるため`met`が二つ存在することになって紛らわしい気がするけど。

```
        des: {
            is: (n, o) => !!getDes(o, n),
            a: {// accessor
                g: (n, o) => hasAcc(o, n, true, false),  // getter
                s: (n, o) => hasAcc(o, n, false, true),  // setter
                gs: (n, o) => hasAcc(o, n, true, true), // getter & setter
            }, 
            d: {// data
                v: (n, o) => isDesV(o, n), //value
                c: (n, o) => isDesC(o, n), //callable
            },
        },
        met: {// 正確に判定したい時はこっちを使えるようにする？
            is: (n, o) => ,
            s: (n, o) => ,
            a: (n, o) => ,
            g: (n, o) => ,
            ag: (n, o) => ,
        }
        cal: {
            met: {
                is: anyMethod,
                f: (v) => isMethod(v),
                a: (v) => isMethod(v, 'Async'),
                g: (v) => isMethod(v, 'Generator'),
                ag: (v) => isMethod(v, 'AsyncGenerator'),
            },
```

`anyArrFn`と`anyMethod`も変えて`isArrStyle`は削除すべきでは？



全コード投稿してみる。

```
const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !isPri(v)
    , isFn = (v) => 'function' === typeof v
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v.toString())
    , isBndStyle = (v) => v.name.startsWith('bound ') && !('prototype' in v)
    , isCls = (v) => isFn(v) &&  isClsStyle(v)
    , isCal = (v) => isFn(v) && !isClsStyle(v)
    , isNtv = (v) => isCal(v) && isNtvStyle(v)
    , isBnd = (v) => isCal(v) && isNtvStyle(v) &&  isBndStyle(v)
    , isBlt = (v) => isCal(v) && isNtvStyle(v) && !isBndStyle(v)
    , AsyncFunction = (async () => {}).constructor
    , GeneratorFunction = (function* () {}).constructor
    , AsyncGeneratorFunction = (async function* () {}).constructor
    , isFun = (v, type) => isCal(v) && !isNtv(v) && v.constructor === type
    , anyFun = (v) => isCal(v) && !!v.prototype && !isNtv(v)
    , isArrOrMethod = (v) => isCal(v) && !v.prototype && !isNtv(v)
    , isArrStyle = (v) => {
        const s = v.toString().trim();
        return /^(async\s+)?(\([^)]*\)|[a-zA-Z_$][\w$]*)\s*=>/.test(s);
    }
    , anyArrFn = (v) => isArrOrMethod(v) && isArrStyle(v)
    , isArrFn = (v, type) => anyArrFn(v) && v.constructor === type // getIdからコンストラクタ比較に変更
    , anyMethod = (v) => isArrOrMethod(v) && !isArrStyle(v)
    , isMethod = (v, type) => anyMethod(v) && v.constructor === type // 同上
    , isObj = (v) => Object===v?.constructor
    , pTypNms = 'Undefined Null Boolean Number BigInt String Symbol'.split(' ')
    , pClsOfNms = {Undefined:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}
    , isAry = (v) => Array.isArray(v)
    , getDes = (o, n) => Object.getOwnPropertyDescriptor(o, n)
    , isDesV = (o, n) => {const d=getDes(o, n); return d && !isFn(d.value)} 
    , isDesC = (o, n) => {const d=getDes(o, n); return d &&  isFn(d.value)}
    , hasAcc = (o, n, g, s) => {
        const d = getDes(o, n);
        return !!(g && d?.get || !g && !d?.get) && (s && d?.set || !s && !d?.set);
    }
;
const pri = {
    isUnd: (v) => undefined===v,
    isNul: (v) => null===v,
    isBln: (v) => 'boolean'===typeof v,
    isNum: (v) => 'number'===typeof v,
    isBig: (v) => 'bigint'===typeof v,
    isStr: (v) => 'string'===typeof v,
    isSym: (v) => 'symbol'===typeof v,
};
const priOfFn = (v, c) => {
    if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
    const n = pTypNms.find(n=>n===c.name);
    if(!n) {throw new TypeError(`cはプリミティブ型であるべきです。実際値:${n} 期待値:${pTypNms}`)}
    return pri[`is${pClsOfNms[n]}`];
};
const insOfFn = (v, c) => {
    if(!isCls(c)){throw new TypeError(`cはClassであるべきです。`)};
    return ((v, c)=>v instanceof c);
};
export const typer = Object.freeze({
    id: getId,
    p: {
        is: isPri,
        of: (v,c) => (priOfFn(v,c))(v,c),
        und: (v) => pri.isUnd,
        nul: (v) => pri.isNul,
        bln: (v) => pri.isBln,
        num: (v) => pri.isNum,
        big: (v) => pri.isBig,
        str: (v) => pri.isStr,
        sym: (v) => pri.isSym,
    }, 
    r: {
        is: isRef,
        own: {
            cls: isCls,
            ins: {
                of: (v, c)=>v instanceof c,
                obj: isObj,
                ary: isAry,
                set: (v) => v instanceof Set, // 'Set'===getId(v),
                map: (v) => v instanceof Map, // 'Map'===getId(v),
                soloObj: (v) => isObj(v) || Object.getPrototypeOf(v) === null,
                blankObj: (v) => isObj(v) && 0===Object.keys(v).length,
                blankAry: (v) => isAry(v) && 0===v.length,
                aryOf: (v, c) => {// Array<T>
                    const fn = isPri(c) ? priOfFn(v,c) : (isCls(c) ? insOfFn(v,c) : (()=>{throw new TypeError(`cはクラスであるべきです。`)})());
                    if (!isAry(v)) {throw new TypeError(`vはArrayであるべきです。`)}
                    return v.every(e => fn(e, c));
                },
            }
        },
        itr: (v) => isRef(v) && isFn(v.next),
        gen: (v) => 'Generator' === getId(v),
        des: {
            is: (n, o) => !!getDes(o, n),
            a: {// accessor
                g: (n, o) => hasAcc(o, n, true, false),  // getter
                s: (n, o) => hasAcc(o, n, false, true),  // setter
                gs: (n, o) => hasAcc(o, n, true, true), // getter & setter
            }, 
            d: {// data
                v: (n, o) => isDesV(o, n), //value
                c: (n, o) => isDesC(o, n), //callable
            },
        },
        cal: {
            is: isCal,
            ntv: {
                is: isNtv,
                bnd: isBnd,
                blt: isBlt,
            },
            fun: {
                is: anyFun,
                f: (v) => isFun(v, Function),
                a: (v) => isFun(v, AsyncFunction),
                g: (v) => isFun(v, GeneratorFunction),
                ag: (v) => isFun(v, AsyncGeneratorFunction),
            },
            arw: {
                is: anyArrFn,
                f: (v) => isArrFn(v, Function),
                a: (v) => isArrFn(v, AsyncFunction),
            },
            met: {
                is: anyMethod,
                f: (v) => isMethod(v, Function),
                a: (v) => isMethod(v, AsyncFunction),
                g: (v) => isMethod(v, GeneratorFunction),
                ag: (v) => isMethod(v, AsyncGeneratorFunction),
            },
        },
        err: {
            cls: (v) => isCls(v) && (v.prototype instanceof Error || v === Error),
            ins: (v) => v instanceof Error, // getId(v).endsWith('Error')
        },
    },
});
```

## 1. p（プリミティブ）層の戻り値

ほんとだ。7つとも以下のように修正した。

```
export const typer = Object.freeze({
    id: getId,
    p: {
        is: isPri,
        of: (v,c) => (priOfFn(v,c))(v,c),
        und: pri.isUnd,
        nul: pri.isNul,
        bln: pri.isBln,
        num: pri.isNum,
        big: pri.isBig,
        str: pri.isStr,
        sym: pri.isSym,
    }, 
```

## 2. typer.p.of の引数

たしかに。以下のように修正した。

```
        of: (v,c) => (priOfFn(v,c))(v),
```

## 3. r.own.ins.aryOf の論理

あーそうだわ。どうするかな。`Number`,`String`などを渡して該当するチェックをしたいのだが、その場合`null`や`undefined`はどうしよう。特に`undefined`は引数なしの時と区別できないからなぁ。

`pTypNms.includes(c.name)`のようにやりたいんだけど、`null`や`undefined`が渡された時は常にfalseかReferenceErrorになるのかな？　どうしよう。当然DRYに書きたいのだが。いい書き方を思いつけない。ジェミえもん、なんかコード出して！

## 4. 記述子 des の引数順

以下のように呼び出しているから大丈夫なはず。

```
        des: {
            is: (n, o) => !!getDes(o, n),
            a: {// accessor
                g: (n, o) => hasAcc(o, n, true, false),  // getter
                s: (n, o) => hasAcc(o, n, false, true),  // setter
                gs: (n, o) => hasAcc(o, n, true, true), // getter & setter
            }, 
            d: {// data
                v: (n, o) => isDesV(o, n), //value
                c: (n, o) => isDesC(o, n), //callable
            },
        },
```



## 1. r.own.ins.aryOf の論理修正案

問題は`null`や`undefined`はプリミティブだからプロパティを持てないことだ。当然`name`なんてプロパティも持てない。よって実行時エラーになるはずだ。

呼出は以下を想定している。

```
typer.r.own.ins.aryOf(v, null); // [null, null] のような配列を期待する（nameプロパティは無いので実行時エラー）
typer.r.own.ins.aryOf(v, undefined); // [undefined, undefined] のような配列を期待する（nameプロパティは無いので実行時エラー）
typer.r.own.ins.aryOf(v); // [undefined, undefined] のような配列を期待する（nameプロパティは無いので実行時エラー）
typer.r.own.ins.aryOf(v, Number); // [1, 2] のような配列を期待する
class C {}
typer.r.own.ins.aryOf(v, C); // [new C(), new C()] のような配列を期待する
```

それともNull,Undefinedクラスを作るか？　わざわざnullやundefinedが存在するのに非公式の型を？

```
class Null {static name = 'Null'}
class Undefined {static name = 'Undefined'}

typer.r.own.ins.aryOf(v, Null); // [null, null] のような配列を期待する
typer.r.own.ins.aryOf(v, Undefined); // [undefined, undefined] のような配列を期待する
typer.r.own.ins.aryOf(v, Number); // [1, 2] のような配列を期待する
class C {}
typer.r.own.ins.aryOf(v, C); // [new C(), new C()] のような配列を期待する
```

しかしこれ、書いてて思ったのだが、インスタンスなのか、それともクラスなのかを区別したいな。でも現状のAPIではインスタンス固定だ。どうしよう。まあ、おまけで作っただけだし、適当でいいか？　いやいや妥協するのは嫌だ。

```
        own: {
            cls: isCls,
            ins: {
                of: (v, c)=>v instanceof c,
                obj: {
                    is: isObj,
                    solo: (v) => isObj(v) || Object.getPrototypeOf(v) === null, // 旧soloObj
                    blank: (v) => isObj(v) && 0===Object.keys(v).length, // 旧blankObj
                },
                ary: {
                    is: isAry,
                    blank: (v) => isAry(v) && 0===v.length, // 旧blankAryはここに移す
                    cls: {
                        of: (v,c)=>,//新しく書く
                    },
                    ins: {
                        of: (v,c)=>,//現状のaryOfコードはここに移す
                    },
                    //新しく書く（プリミティブ版）
                    und: {is: (v)=>},
                    nul: {is: (v)=>},
                    bln: {is: (v)=>},
                    num: {is: (v)=>},
                    big: {is: (v)=>},
                    str: {is: (v)=>},
                    sym: {is: (v)=>},
                    // 他の全部も欲しくなり膨大になってしまう。ジェネリクスが欲しいだけなのに。DRYに書くならProxyを使うしか無いか？
                },
                set: (v) => v instanceof Set, // 'Set'===getId(v),
                map: (v) => v instanceof Map, // 'Map'===getId(v),
            }
```








    , fns = [Function,AsyncFunction,GeneratorFunction,AsyncGeneratorFunction]



```
const getId = (v) => Object.prototype.toString.call(v).slice(8, -1)
    , isPri = (v) => null === v || v !== Object(v)
    , isRef = (v) => !isPri(v) // Pri から isPri に修正
    , isFn = (v) => 'function' === typeof v
    , isClsStyle = (v) => /^class\b/.test(v?.toString?.()?.trim() ?? '')
    , isNtvStyle = (v) => /\[native code\]/.test(v?.toString?.() ?? '')
    , isBndStyle = (v) => v.name.startsWith('bound ') && !('prototype' in v) // in に修正
    , isCls = (v) => isFn(v) && isClsStyle(v)
    , isCal = (v) => isFn(v) && !isClsStyle(v)
    , isNtv = (v) => isCal(v) && isNtvStyle(v)
    , isBnd = (v) => isCal(v) && isNtvStyle(v) && isBndStyle(v)
    , isBlt = (v) => isCal(v) && isNtvStyle(v) && !isBndStyle(v)
    // 構文エラーを修正した「種」
    , AsyncFunction = (async function () {}).constructor
    , GeneratorFunction = (function* () {}).constructor
    , AsyncGeneratorFunction = (async function* () {}).constructor
    , isFun = (v, type) => isCal(v) && !isNtv(v) && v.constructor === type
    , anyFun = (v) => isCal(v) && !!v.prototype && !isNtv(v)
    , isArrOrMethod = (v) => isCal(v) && !v.prototype && !isNtv(v)
    , isArrStyle = (v) => {
        const s = v.toString().trim();
        return /^(async\s+)?(\([^)]*\)|[a-zA-Z_$][\w$]*)\s*=>/.test(s);
    }
    , anyArrFn = (v) => isArrOrMethod(v) && isArrStyle(v)
    , isArrFn = (v, type) => anyArrFn(v) && v.constructor === type // getIdからコンストラクタ比較に変更
    , anyMethod = (v) => isArrOrMethod(v) && !isArrStyle(v)
    , isMethod = (v, type) => anyMethod(v) && v.constructor === type // 同上
    , isObj = (v) => Object === v?.constructor
    , pTypNms = 'Undefined Null Boolean Number BigInt String Symbol'.split(' ') // eを追加
    , pClsOfNms = {Undefined:'Und', Null:'Nul', Boolean:'Bln', Number:'Num', BigInt:'Big', String:'Str', Symbol:'Sym'}
;
```






さて、これで問題ないなら、次は機能拡張の前に単体テストだな。



```
Reference
    Constructable (Owner)
        Class           (ES6 class構文)
        BuiltinConstructor (Date, RegExpなどの組み込み。isBltかつnew可能)
        LegacyConstructor  (ES5までの function A(){}。判定不能だがFunctionから分離したい意図)
    Instance            (newで生成されたオブジェクト。Object, Array等を除く)
    Collection
        Object          ({} / nullプロトタイプ)
        Array           ([])
        TypedArray      (Uint8Array等)
        Set / Map / WeakSet / WeakMap
    Callable (Executableより改名)
        NativeCode      (Bound / Builtin)
        Functionable    (Function / Async / Generator / AsyncGen)
        Arrowable       (Arrow / AsyncArrow)
        Methodable      (Method / Async / Gen / AsyncGen)
    Special
        Promise
        Error
        Iterator / Generator (実行中のイテレータ)
```

```
Primitive
	Undefined
	Null
	Boolean
	Number
	BigInt
	String
	Symbol
Reference
	Owner
		Class
		Instance
		Object		{}で生成されたもののみ
		Array
		Set
		Map
		Iterator
		Generator
	Executable
		Function（組合せ4種）
			isAsync
			isGenerator
		ArrowFunction（組合せ2種）
			isAsync
		BoundFunction
		NativeCode
		Methodable（組合せ4種）
			isAsync
			isGenerator
```




ということは実際は以下のようになるということか？

```
Primitive
	Undefined
	Null
	Boolean
	Number
	BigInt
	String
	Symbol
Executable = Reference
	Class
	Instance
	Object		{}で生成されたもののみ
	Array
	Set
	Map
	Iterator
	Generator
	Functionable
		Function
		AsyncFunction
		GeneratorFunction
		AsyncGeneratorFunction
	ArrowFunctionable
		ArrowFunction
		AsyncArrowFunction
	BoundFunction
	NativeCode
	Methodable
		Method
		AsyncMethod
		GeneratorMethod
		AsyncGeneratorMethod

```



```
Class
Executable
	Function
	ArrowFunction
	Bound
	NativeCode
	Method
```


